#### Static Routing Configuration for The Microservices (Suppose we Know Address for each of Them ) #######
spring:
  cloud:
    gateway:
      mvc:
        routes:
          - id : r1
            uri: http://localhost:8081/
            predicates:
              - Path= /api/customers/**
          - id: r2
            uri: http://localhost:9090/
            predicates:
              - Path= /products_api/products/**
server:
  port: 8888